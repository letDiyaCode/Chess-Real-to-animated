# Chess Real-to-Animated — Python dependencies
# Cross-platform (macOS/Linux/Windows). Tested with Python 3.11.
# Install:  python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Core ML / CV
tensorflow==2.20.0
opencv-python==4.12.0.88
numpy==2.2.6
scikit-learn==1.7.2

# Image handling (incl. iPhone HEIC support)
pillow==11.3.0
pillow-heif==1.1.0

# Data / plotting
pandas==2.3.3
matplotlib==3.10.7

# Chess logic (move validation, FEN/PGN) — used by move_detector
python-chess==1.999

# Utilities
tqdm==4.67.1
